<form method="post" action="{{ path('backend_actualite_delete', {'id': actualite.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette actualité?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actualite.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
