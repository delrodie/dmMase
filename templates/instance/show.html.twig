{% extends 'base.html.twig' %}

{% block title %}Instance{% endblock %}

{% block body %}
    <h1>Instance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instance.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ instance.libelle }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ instance.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('instance_index') }}">back to list</a>

    <a href="{{ path('instance_edit', {'id': instance.id}) }}">edit</a>

    {{ include('instance/_delete_form.html.twig') }}
{% endblock %}
